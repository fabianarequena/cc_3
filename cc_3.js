// Task 1: Product Price Management

let prices = [100, 200, 300, 400, 500];
prices.push(250);  // Adding a new price to the array
prices.shift();    // Removing the first price from the array

console.log("Updated Prices:", prices);
